# OpenEvolve

This is a fork of [OpenEvolve](https://github.com/algorithmicsuperintelligence), which is extended to support the notion of multiple lanes of related algorithm lineages that advance in parallel in a single evolutionary context, so that they incorporate ideas generated in other lanes while optimizing for different objectives.
